package com.notification.app.ui.designsystem

/**
 * Sprint 1 — Application Foundation / Design System.
 *
 * Reusable animation duration scale (in milliseconds) so transitions,
 * fades, and press-states feel consistent across the new screens.
 * This does NOT change the existing NavHost transition timings in
 * MainActivity — those are left untouched.
 */
object AppAnimationDuration {
    const val fast = 120
    const val normal = 220
    const val slow = 320
}
