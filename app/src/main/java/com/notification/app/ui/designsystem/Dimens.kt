package com.notification.app.ui.designsystem

import androidx.compose.ui.unit.dp

/**
 * Sprint 1 — Application Foundation / Design System.
 *
 * Reusable sizing tokens for common UI elements (icons, avatars, bars,
 * buttons). Introduces no new colors or typography — pure dimensions.
 */
object AppDimens {
    // Icons
    val iconSizeSmall = 18.dp
    val iconSizeMedium = 24.dp
    val iconSizeLarge = 32.dp

    // Avatars / profile button in the Top App Bar
    val avatarSizeSmall = 32.dp
    val avatarSizeMedium = 40.dp

    // Bars
    val topBarHeight = 64.dp
    val topBarHeightCompact = 56.dp
    val bottomNavHeight = 80.dp

    // FAB
    val fabIconSize = 26.dp

    // Buttons
    val buttonHeight = 48.dp
    val buttonHeightCompact = 40.dp

    // Cards
    val quickActionCardSize = 88.dp
}
